//facture =>date de payment,

// si la facture est validé le stck doit duminuer

// devis => date d'expiration

// distinguerles categorie de produits

//   //$table->enum('validation', ['non', 'oui'])->default('non');  
//   :de le definir comme activer le stock
